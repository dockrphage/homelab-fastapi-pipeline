apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - configmap.yaml
  - secret.yaml
  - ingress.yaml

namespace: uat

patches:
  - path: patch-deployment.yaml
    target:
      kind: Deployment
      name: fastapi-app
